import 'package:flutter/material.dart';

class ParkPage extends StatelessWidget {
  const ParkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('彼岸公园'),
      ),
      body: const Center(
        child: Text('彼岸公园 - 开发中'),
      ),
    );
  }
}
